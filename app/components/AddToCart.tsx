'use client'
import React from 'react'

const AddToCart = () => {
  return (
    <button className='btn btn-primary border' onClick={() => console.log('add to cart')}>Add</button>
  )
}

export default AddToCart;